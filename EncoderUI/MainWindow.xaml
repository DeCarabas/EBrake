<Window x:Class="EncoderUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="MainWindow" Height="709" Width="799">

    <Window.Resources>
        <Canvas x:Key="appbar_arrow_right" Width="48" Height="48" Clip="F1 M 0,0L 48,0L 48,48L 0,48L 0,0" UseLayoutRounding="False">
            <Path Width="25" Height="18" Canvas.Left="12" Canvas.Top="15" Stretch="Fill" Fill="#FF000000" 
                  Data="F1 M 12,22L 12,26L 28.25,26L 21,33L 27.5,33L 37,24L 27.5,15L 21,15L 28.25,22L 12,22 Z "/>
        </Canvas>

        <Canvas x:Key="appbar_arrow_down" Width="48" Height="48" Clip="F1 M 0,0L 48,0L 48,48L 0,48L 0,0" UseLayoutRounding="False">
            <Path Width="18" Height="25" Canvas.Left="15" Canvas.Top="12" Stretch="Fill" Fill="#FF000000" 
                  Data="F1 M 22,12L 26,12L 26,28.25L 33,21L 33,27.5L 24,37L 15,27.5L 15,21L 22,28.25L 22,12 Z "/>
        </Canvas>

        <SolidColorBrush x:Key="WarningBrush" Color="#FFFFE1" />

        <Style TargetType="Button" x:Key="WarningBar">
            <Setter Property="Background" Value="{StaticResource WarningBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Height" Value="auto" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="Button">
                    <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>

                            <Grid Background="#02FFFFFF" Grid.Column="1" Height="25" Width="25">
                                <Ellipse x:Name="ellipsebg"
                                 Margin="4"
                                 Stroke="{TemplateBinding Foreground}"
                                 StrokeThickness="0"
                                 VerticalAlignment="Stretch" />
                                <Ellipse x:Name="ellipse"
                                 Margin="4"
                                 Stroke="{TemplateBinding Foreground}"
                                 StrokeThickness="2"
                                 VerticalAlignment="Stretch" />
                                <Rectangle Width="10" Height="10">
                                    <Rectangle.Fill>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_arrow_right}" />
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Grid>
                        
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />                                                
                    </Grid>
                  </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="entryLabel">
            <Setter Property="VerticalAlignment" Value="Top" />            
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" Color="Black" BlurRadius="15" />
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="0,0,0,40" />
        </Style>
        <Style TargetType="TextBox" x:Key="textEntry">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
        <Style TargetType="TextBlock" x:Key="textValue">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
    </Window.Resources>

    <Controls:MetroContentControl>
        <Grid>
            <TabControl x:Name="Tabs">
                <TabItem Header="Movie">
                    <Controls:MetroContentControl>
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5" 
                              DataContext="{Binding Path=MovieEncodeInfo}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Image Grid.RowSpan="5" Source="/EncoderUI;component/Popcorn.jpg" Stretch="UniformToFill" />

                            <Button Click="Settings_Click" Style="{StaticResource WarningBar}">
                                Some settings are missing. Click here to go to the settings tab.
                            </Button>
                            <TextBox Grid.Row="1" Text="{Binding Path=MovieTitle}" Margin="10" TextAlignment="Center" 
                                     FontSize="24" />
                            <TextBlock Grid.Row="2" Style="{StaticResource entryLabel}" Text="Movie Title" />

                            <TextBox Grid.Row="3" Text="{Binding Path=MovieYear}" Margin="10" TextAlignment="Center" 
                                     FontSize="24" />
                            <TextBlock Grid.Row="4" Style="{StaticResource entryLabel}" Text="Movie Year" />

                            <ProgressBar Grid.Row="5" />
                            <TextBlock Grid.Row="5" />

                            <Button Grid.Row="6" Style="{DynamicResource MetroCircleButtonStyle}" 
                                    Height="55" Width="55"
                                    Click="StartButton_Click">                                
                                <Rectangle Width="20" Height="20">
                                    <Rectangle.Fill>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_arrow_down}" />
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Button>
                        </Grid>
                    </Controls:MetroContentControl>
                </TabItem>
                <TabItem Header="TV Series">
                    <Controls:MetroContentControl>
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="111*" />
                                <ColumnDefinition Width="301*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Style="{StaticResource entryLabel}" Text="Series Title" />
                            <TextBlock Grid.Row="1" Style="{StaticResource entryLabel}" Text="Season" />
                            <TextBlock Grid.Row="2" Style="{StaticResource entryLabel}" Text="DVD Drive" />
                            <TextBlock Grid.Row="3" Style="{StaticResource entryLabel}" Text="Destination" />
                        </Grid>
                    </Controls:MetroContentControl>
                </TabItem>
                <TabItem Header="Settings" x:Name="SettingsTab">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Style="{StaticResource entryLabel}" Text="Source DVD" />
                        <ComboBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" 
                                        VerticalAlignment="Center" ItemsSource="{Binding Path=OpticalDrives}" 
                                        SelectedItem="{Binding Path=SourceDrive}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <WrapPanel>
                                        <TextBlock Text="{Binding RootDirectory}" />
                                        <TextBlock Text="{Binding VolumeLabel}" Margin="10,0,0,0" />
                                    </WrapPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <TextBlock Grid.Row="1" Style="{StaticResource entryLabel}" Text="Destination" />
                        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource textEntry}" Text="{Binding Path=OutputPath}" />
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Controls:MetroContentControl>        
</Window>
